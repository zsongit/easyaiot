# ============================================
# AI服务环境变量配置
# ============================================
# 注意：此文件包含敏感信息，不要提交到版本控制系统
# ============================================

# ============================================
# Flask应用配置
# ============================================
# Flask密钥（用于会话加密）
SECRET_KEY=8595796f2b6c5aefbc2f0d0442d9fcf7c4142ae27fa2bf14acc2f2b88352a2e35b0c35d17006709a

# Flask运行端口（默认5000）
FLASK_RUN_PORT=5000

# Flask运行主机（默认0.0.0.0）
FLASK_RUN_HOST=0.0.0.0

# Flask调试模式（生产环境建议设置为False）
DEBUG=False

# 允许的主机列表
ALLOWED_HOSTS=[*]

# ============================================
# 数据库配置
# ============================================
# PostgreSQL数据库连接URL
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# 注意：如果使用postgres://开头，代码会自动转换为postgresql://
# 生产环境数据库地址
DATABASE_URL=postgresql://postgres:iot45722414822@iot.basiclab.top:5432/iot-ai10

# ============================================
# Nacos服务注册与发现配置
# ============================================
# Nacos服务器地址（格式：IP:端口）
NACOS_SERVER=iot.basiclab.top:8848

# Nacos命名空间（默认：local）
NACOS_NAMESPACE=dev

# Nacos用户名（默认：nacos）
NACOS_USERNAME=nacos

# Nacos密码
NACOS_PASSWORD=basiclab@iot78475418754

# 服务名称（用于Nacos注册）
SERVICE_NAME=model-server

# Pod IP地址（可选，如果不设置会自动获取）
# 在Docker容器中运行时，建议设置为容器的IP地址
POD_IP=

# ============================================
# MinIO对象存储配置
# ============================================
# MinIO服务器地址（格式：IP:端口）
MINIO_ENDPOINT=iot.basiclab.top:9000

# MinIO访问密钥
MINIO_ACCESS_KEY=minio_root

# MinIO密钥
MINIO_SECRET_KEY=minio_123456

# MinIO是否使用HTTPS（true/false）
MINIO_SECURE=false

# ============================================
# GPU配置
# ============================================
# CUDA可见设备（0表示使用第一个GPU，all表示使用所有GPU）
# 在docker-compose.yaml中已配置，这里可以覆盖
CUDA_VISIBLE_DEVICES=0

# ============================================
# 讯飞语音识别配置（可选）
# ============================================
# 讯飞APP ID（如果不需要语音识别功能，可以留空）
XUNFEI_APP_ID=

# 讯飞密钥（如果不需要语音识别功能，可以留空）
XUNFEI_SECRET_KEY=

# ============================================
# 推流服务器配置（可选）
# ============================================
# 推流服务器地址（用于推理结果推流）
MODEL_AI_PUSH_URL=pro.basiclab.top:1935

# ============================================
# 其他配置
# ============================================
# Python输出不缓冲（建议设置为1）
PYTHONUNBUFFERED=1

# 时区设置
TZ=Asia/Shanghai

# ============================================
# Redis配置（可选，如果需要）
# ============================================
# Redis服务器地址（生产环境）
# REDIS_HOST=iot.basiclab.top
# REDIS_PORT=6379
# REDIS_PASSWORD=basiclab@iot975248395

# ============================================
# Kafka配置（可选，如果需要）
# ============================================
# Kafka服务器地址（生产环境）
# KAFKA_BOOTSTRAP_SERVERS=iot.basiclab.top:9092

# ============================================
# TDengine配置（可选，如果需要）
# ============================================
# TDengine服务器地址（生产环境）
# TDENGINE_HOST=iot.basiclab.top
# TDENGINE_PORT=6030

# ============================================
# 代理配置（可选，如果需要）
# ============================================
# HTTP_PROXY=
# HTTPS_PROXY=
# NO_PROXY=localhost,127.0.0.1
