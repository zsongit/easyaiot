# Документация по развертыванию платформы EasyAIoT

## 📋 Содержание

- [Обзор](#обзор)
- [Требования к окружению](#требования-к-окружению)
- [Быстрый старт](#быстрый-старт)
- [Инструкция по использованию скриптов](#инструкция-по-использованию-скриптов)
- [Описание модулей](#описание-модулей)
- [Порты сервисов](#порты-сервисов)
- [Частые проблемы](#частые-проблемы)
- [Управление журналами](#управление-журналами)

## Обзор

EasyAIoT — это платформа облачно-периферийных интеллектуальных алгоритмических приложений, использующая единый скрипт установки для развертывания в один клик. Платформа поддерживает контейнеризированное развертывание через Docker и позволяет быстро установить и запустить все сервисные модули.

### Архитектура платформы

Платформа EasyAIoT состоит из следующих ключевых модулей:

- **Базовые сервисы** (`.scripts/docker`): Включают промежуточное ПО, такое как Nacos, PostgreSQL, Redis, TDEngine, Kafka, MinIO и другие.
- **DEVICE сервис**: Сервис управления устройствами и шлюза (на основе Java).
- **AI сервис**: Сервис обработки искусственного интеллекта (на основе Python).
- **VIDEO сервис**: Сервис обработки видео (на основе Python).
- **WEB сервис**: Веб-интерфейс (на основе Vue).

## Требования к окружению

### Системные требования

- **Операционная система**:
   - Linux (рекомендуется Ubuntu 24.04)
   - macOS (рекомендуется macOS 10.15+)
   - Windows (рекомендуется Windows 10/11, требуется PowerShell 5.1+)
- **Память**: Рекомендуется 32 ГБ (минимум 16 ГБ)
- **Дисковое пространство**: Рекомендуется 50 ГБ и более свободного места
- **CPU**: Рекомендуется 8 ядер (минимум 4 ядра)
- **Видеокарта**: Рекомендуется NVIDIA GPU (минимум CPU)

### Программные зависимости

Перед запуском скрипта развертывания убедитесь, что установлено следующее ПО:

1. **Docker** (обязательная версия v29.0.0+)
   - Инструкция по установке: https://docs.docker.com/get-docker/
   - Проверка установки: `docker --version`
   - **Важно**: Версия Docker должна быть v29.0.0 или выше, более ранние версии не будут работать корректно.

2. **Docker Compose** (обязательная версия v2.35.0+)
   - Инструкция по установке: https://docs.docker.com/compose/install/
   - Проверка установки: `docker compose version`
   - **Важно**: Версия Docker Compose должна быть v2.35.0 или выше, более ранние версии не будут работать корректно.

3. **Прочие зависимости**:
   - **Linux/macOS**: `curl` (используется для проверки работоспособности, обычно предустановлен в системе)
   - **Windows**: PowerShell 5.1+ (обычно предустановлен в системе)

### Настройка прав доступа Docker

#### Linux

Убедитесь, что текущий пользователь имеет доступ к Docker daemon:

```bash
# Способ 1: Добавить пользователя в группу docker (рекомендуется)
sudo usermod -aG docker $USER
# Затем перелогиньтесь или выполните
newgrp docker

# Способ 2: Запускать скрипт через sudo (не рекомендуется)
sudo ./install_linux.sh [команда]
```

Проверка прав доступа Docker:

```bash
docker ps
```

#### macOS

В macOS обычно не требуется специальной настройки прав, Docker Desktop обрабатывает их автоматически.

#### Windows

В Windows Docker Desktop также автоматически обрабатывает права, но при необходимости убедитесь, что PowerShell запущен от имени администратора.

## Быстрый старт

### Развертывание в Linux

#### 1. Получение кода проекта

```bash
# Клонирование проекта (если еще не сделано)
git clone <repository-url>
cd easyaiot
```

#### 2. Переход в директорию со скриптами

```bash
cd .scripts/docker
```

#### 3. Выдача прав на выполнение скрипта

```bash
chmod +x install_linux.sh
```

#### 4. Установка всех сервисов в один клик

```bash
./install_linux.sh install
```

Эта команда выполнит:
- Проверку окружения Docker и Docker Compose
- Создание единой сети `easyaiot-network`
- Установку всех модулей в порядке зависимостей
- Запуск всех сервисных контейнеров

#### 5. Проверка статуса сервисов

```bash
./install_linux.sh verify
```

Если все сервисы работают нормально, будут показаны адреса для доступа к ним.

### Развертывание в macOS

#### 1. Получение кода проекта

```bash
# Клонирование проекта (если еще не сделано)
git clone <repository-url>
cd easyaiot
```

#### 2. Переход в директорию со скриптами

```bash
cd .scripts/docker
```

#### 3. Выдача прав на выполнение скрипта

```bash
chmod +x install_mac.sh
```

#### 4. Установка всех сервисов в один клик

```bash
./install_mac.sh install
```

Эта команда выполнит:
- Проверку окружения Docker и Docker Compose
- Создание единой сети `easyaiot-network`
- Установку всех модулей в порядке зависимостей
- Запуск всех сервисных контейнеров

#### 5. Проверка статуса сервисов

```bash
./install_mac.sh verify
```

Если все сервисы работают нормально, будут показаны адреса для доступа к ним.

### Развертывание в Windows

Для получения подробных инструкций по развертыванию в Windows, включая подготовку окружения, развертывание промежуточного программного обеспечения, запуск сервисов и устранение неполадок, обратитесь к специальному [Руководству по развертыванию Windows](平台Windows部署文档_ru.md).

Руководство по развертыванию Windows предоставляет подробные пошаговые инструкции, охватывающие:
- Системные требования и настройка окружения
- Установка и настройка промежуточного программного обеспечения
- Процедуры запуска сервисов
- Настройка видеопотоков
- Распространенные проблемы и решения
- Справочник команд

Это руководство специально разработано для систем Windows 10/11 и охватывает сценарии локального развертывания.

## Инструкция по использованию скриптов

### Расположение скриптов

Единые скрипты установки находятся в директории `.scripts/docker/` корня проекта:

- **Linux**: `install_linux.sh`
- **macOS**: `install_mac.sh`
- **Windows**: `install_win.ps1`

### Доступные команды

Все операционные системы поддерживают одинаковые команды, но имена скриптов различаются:

| Команда | Описание | Пример для Linux | Пример для macOS | Пример для Windows |
|---------|----------|------------------|------------------|--------------------|
| `install` | Установить и запустить все сервисы (первый запуск) | `./install_linux.sh install` | `./install_mac.sh install` | `.\install_win.ps1 install` |
| `start` | Запустить все сервисы | `./install_linux.sh start` | `./install_mac.sh start` | `.\install_win.ps1 start` |
| `stop` | Остановить все сервисы | `./install_linux.sh stop` | `./install_mac.sh stop` | `.\install_win.ps1 stop` |
| `restart` | Перезапустить все сервисы | `./install_linux.sh restart` | `./install_mac.sh restart` | `.\install_win.ps1 restart` |
| `status` | Посмотреть статус всех сервисов | `./install_linux.sh status` | `./install_mac.sh status` | `.\install_win.ps1 status` |
| `logs` | Посмотреть журналы всех сервисов | `./install_linux.sh logs` | `./install_mac.sh logs` | `.\install_win.ps1 logs` |
| `build` | Пересобрать все образы | `./install_linux.sh build` | `./install_mac.sh build` | `.\install_win.ps1 build` |
| `clean` | Очистить все контейнеры и образы (опасная операция) | `./install_linux.sh clean` | `./install_mac.sh clean` | `.\install_win.ps1 clean` |
| `update` | Обновить и перезапустить все сервисы | `./install_linux.sh update` | `./install_mac.sh update` | `.\install_win.ps1 update` |
| `verify` | Проверить, успешно ли запущены все сервисы | `./install_linux.sh verify` | `./install_mac.sh verify` | `.\install_win.ps1 verify` |

### Подробное описание команд

#### install - Установка сервисов

Используется при первом развертывании для установки и запуска всех сервисных модулей:

**Linux/macOS**:
```bash
./install_linux.sh install    # Linux
./install_mac.sh install       # macOS
```

**Windows**:
```powershell
.\install_win.ps1 install
```

**Процесс выполнения**:
1. Проверка окружения Docker и Docker Compose
2. Создание Docker сети `easyaiot-network`
3. Установка модулей в порядке зависимостей:
   - Базовые сервисы (Nacos, PostgreSQL, Redis и др.)
   - DEVICE сервис
   - AI сервис
   - VIDEO сервис
   - WEB сервис
4. Отображение статистики результатов установки

#### start - Запуск сервисов

Запуск всех установленных сервисов:

**Linux/macOS**:
```bash
./install_linux.sh start    # Linux
./install_mac.sh start      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 start
```

#### stop - Остановка сервисов

Остановка всех работающих сервисов (в обратном порядке):

**Linux/macOS**:
```bash
./install_linux.sh stop    # Linux
./install_mac.sh stop      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 stop
```

#### restart - Перезапуск сервисов

Перезапуск всех сервисов:

**Linux/macOS**:
```bash
./install_linux.sh restart    # Linux
./install_mac.sh restart      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 restart
```

#### status - Просмотр статуса

Просмотр статуса работы всех сервисов:

**Linux/macOS**:
```bash
./install_linux.sh status    # Linux
./install_mac.sh status      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 status
```

#### logs - Просмотр журналов

Просмотр журналов всех сервисов (последние 100 строк):

**Linux/macOS**:
```bash
./install_linux.sh logs    # Linux
./install_mac.sh logs      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 logs
```

#### build - Сборка образов

Пересборка Docker образов всех сервисов (с использованием опции `--no-cache`):

**Linux/macOS**:
```bash
./install_linux.sh build    # Linux
./install_mac.sh build      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 build
```

**Примечание**: Процесс сборки может занять длительное время, пожалуйста, ожидайте.

#### clean - Очистка сервисов

**⚠️ Опасная операция**: Удаление всех контейнеров, образов и томов данных.

**Linux/macOS**:
```bash
./install_linux.sh clean    # Linux
./install_mac.sh clean      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 clean
```

Перед выполнением будет запрошено подтверждение, введите `y` или `Y` для продолжения, любой другой ввод отменит операцию.

**Что будет удалено**:
- Все сервисные контейнеры
- Все сервисные образы
- Все тома данных
- Docker сеть `easyaiot-network`

#### update - Обновление сервисов

Загрузка последних образов и перезапуск всех сервисов:

**Linux/macOS**:
```bash
./install_linux.sh update    # Linux
./install_mac.sh update      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 update
```

**Процесс выполнения**:
1. Загрузка последних образов для каждого модуля
2. Перезапуск всех сервисов для использования новых образов

#### verify - Проверка сервисов

Проверка, что все сервисы успешно запущены и доступны:

**Linux/macOS**:
```bash
./install_linux.sh verify    # Linux
./install_mac.sh verify      # macOS
```

**Windows**:
```powershell
.\install_win.ps1 verify
```

**Что проверяется**:
- Доступность портов сервисов
- Корректный ответ от эндпоинтов проверки работоспособности
- Отображение адресов для доступа к сервисам

**Пример успешного вывода**:
```
[SUCCESS] Все сервисы работают нормально!

Адреса для доступа к сервисам:
  Базовые сервисы (Nacos):     http://localhost:8848/nacos
  Базовые сервисы (MinIO):     http://localhost:9000 (API), http://localhost:9001 (Console)
  DEVICE сервис (Gateway):  http://localhost:48080
  AI сервис:                http://localhost:5000
  VIDEO сервис:             http://localhost:6000
  WEB интерфейс:               http://localhost:8888
```

## Описание модулей

### Базовые сервисы (`.scripts/docker`)

**Описание**: Содержат все необходимые промежуточные сервисы для работы платформы.

**Включенные сервисы**:
- **Nacos**: Сервис регистрации и конфигурации.
- **PostgreSQL**: Реляционная база данных.
- **Redis**: Кэширующая база данных.
- **TDEngine**: Временная база данных.
- **Kafka**: Очередь сообщений.
- **MinIO**: Сервис объектного хранилища.

**Способ развертывания**:
- **Linux**: С использованием скрипта `install_middleware_linux.sh`.
- **macOS**: С использованием скрипта `install_middleware_mac.sh`.
- **Windows**: С использованием скрипта `install_middleware_win.ps1`.

### DEVICE сервис

**Описание**: Сервис управления устройствами и шлюза, предоставляющий функции подключения устройств, управления продуктами, разметки данных, движка правил и другие.

**Технологический стек**: Java (Spring Cloud).

**Способ развертывания**:
- **Linux**: С использованием скрипта `install_linux.sh`.
- **macOS**: С использованием скрипта `install_mac.sh`.
- **Windows**: С использованием скрипта `install_win.ps1`.

**Основные функции**:
- Управление устройствами.
- Управление продуктами.
- Разметка данных.
- Движок правил.
- Магазин алгоритмов.
- Управление системой.

### AI сервис

**Описание**: Сервис обработки искусственного интеллекта, отвечает за анализ видео и выполнение AI алгоритмов.

**Технологический стек**: Python.

**Способ развертывания**:
- **Linux**: С использованием скрипта `install_linux.sh`.
- **macOS**: С использованием скрипта `install_mac.sh`.
- **Windows**: С использованием скрипта `install_win.ps1`.

**Основные функции**:
- Анализ видео.
- Выполнение AI алгоритмов.
- Вывод моделей.

### VIDEO сервис

**Описание**: Сервис обработки видео, отвечает за обработку и передачу видеопотоков.

**Технологический стек**: Python.

**Способ развертывания**:
- **Linux**: С использованием скрипта `install_linux.sh`.
- **macOS**: С использованием скрипта `install_mac.sh`.
- **Windows**: С использованием скрипта `install_win.ps1`.

**Основные функции**:
- Обработка видеопотоков.
- Передача видео.
- Сервис потокового мультимедиа.

### WEB сервис

**Описание**: Веб-интерфейс, предоставляет пользовательский интерфейс.

**Технологический стек**: Vue.js.

**Способ развертывания**:
- **Linux**: С использованием скрипта `install_linux.sh`.
- **macOS**: С использованием скрипта `install_mac.sh`.
- **Windows**: С использованием скрипта `install_win.ps1`.

**Основные функции**:
- Пользовательский интерфейс.
- Визуализация данных.
- Интерфейс управления системой.

## Порты сервисов

| Модуль сервиса | Порт | Описание | Адрес доступа |
|----------------|------|----------|---------------|
| Nacos | 8848 | Сервис регистрации и конфигурации | http://localhost:8848/nacos |
| MinIO API | 9000 | API объектного хранилища | http://localhost:9000 |
| MinIO Console | 9001 | Консоль объектного хранилища | http://localhost:9001 |
| DEVICE Gateway | 48080 | Шлюз сервиса устройств | http://localhost:48080 |
| AI сервис | 5000 | Сервис AI обработки | http://localhost:5000 |
| VIDEO сервис | 6000 | Сервис обработки видео | http://localhost:6000 |
| WEB интерфейс | 8888 | Веб-интерфейс | http://localhost:8888 |

### Эндпоинты проверки работоспособности

Эндпоинты проверки работоспособности для каждого сервиса:

| Модуль сервиса | Эндпоинт проверки работоспособности |
|----------------|-------------------------------------|
| Базовые сервисы (Nacos) | `/nacos/actuator/health` |
| DEVICE сервис | `/actuator/health` |
| AI сервис | `/actuator/health` |
| VIDEO сервис | `/actuator/health` |
| WEB сервис | `/health` |

## Частые проблемы

### 1. Проблемы с правами доступа Docker

**Проблема**: При выполнении скрипта выводится сообщение "Нет прав доступа к Docker daemon".

**Решение**:

**Linux**:
```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь или выполните
newgrp docker

# Проверка прав
docker ps
```

**macOS**:
В macOS обычно не требуется специальной настройки, просто убедитесь, что Docker Desktop запущен.

**Windows**:
В Windows Docker Desktop автоматически обрабатывает права, убедитесь, что Docker Desktop запущен.

### 2. Занятость порта

**Проблема**: При запуске сервиса выводится сообщение, что порт уже занят.

**Решение**:

**Linux**:
```bash
# Проверить, какой процесс занимает порт
sudo netstat -tulpn | grep <номер порта>
# или
sudo lsof -i :<номер порта>

# Остановить процесс, занимающий порт, или изменить порт в конфигурации сервиса
```

**macOS**:
```bash
# Проверить, какой процесс занимает порт
lsof -i :<номер порта>

# Остановить процесс, занимающий порт, или изменить порт в конфигурации сервиса
```

**Windows**:
```powershell
# Проверить, какой процесс занимает порт
netstat -ano | findstr :<номер порта>

# Остановить процесс, занимающий порт, или изменить порт в конфигурации сервиса
```

### 3. Сбой запуска сервиса

**Проблема**: Не удается запустить какой-либо сервисный модуль.

**Решение**:

**Linux/macOS**:
```bash
# 1. Посмотреть журналы сервисов
./install_linux.sh logs    # Linux
./install_mac.sh logs      # macOS

# 2. Посмотреть подробные журналы конкретного модуля
cd <директория модуля>
docker-compose logs

# 3. Проверить ресурсы Docker
docker ps -a
docker images

# 4. Проверить сеть
docker network ls
docker network inspect easyaiot-network
```

**Windows**:
```powershell
# 1. Посмотреть журналы сервисов
.\install_win.ps1 logs

# 2. Посмотреть подробные журналы конкретного модуля
cd <директория модуля>
docker-compose logs

# 3. Проверить ресурсы Docker
docker ps -a
docker images

# 4. Проверить сеть
docker network ls
docker network inspect easyaiot-network
```

### 4. Сбой сборки образа

**Проблема**: Сбой при сборке образа.

**Решение**:

**Linux/macOS**:
```bash
# 1. Проверить дисковое пространство Docker
docker system df

# 2. Очистить неиспользуемые ресурсы
docker system prune -a

# 3. Проверить сетевое подключение (если требуется загрузка базовых образов)
ping registry-1.docker.io

# 4. Собрать образ для модуля отдельно
cd <директория модуля>
docker-compose build --no-cache
```

**Windows**:
```powershell
# 1. Проверить дисковое пространство Docker
docker system df

# 2. Очистить неиспользуемые ресурсы
docker system prune -a

# 3. Проверить сетевое подключение (если требуется загрузка базовых образов)
Test-NetConnection registry-1.docker.io -Port 443

# 4. Собрать образ для модуля отдельно
cd <директория модуля>
docker-compose build --no-cache
```

### 5. Невозможность доступа к сервису

**Проблема**: Сервис запущен, но недоступен через браузер.

**Решение**:

**Linux**:
```bash
# 1. Проверить, корректно ли работают сервисы
./install_linux.sh verify

# 2. Проверить настройки брандмауэра
sudo ufw status
# Если необходимо открыть порт
sudo ufw allow <номер порта>

# 3. Проверить журналы сервисов
./install_linux.sh logs

# 4. Проверить статус контейнеров
docker ps
```

**macOS**:
```bash
# 1. Проверить, корректно ли работают сервисы
./install_mac.sh verify

# 2. Проверить настройки брандмауэра (Системные настройки > Безопасность и конфиденциальность > Брандмауэр)

# 3. Проверить журналы сервисов
./install_mac.sh logs

# 4. Проверить статус контейнеров
docker ps
```

**Windows**:
```powershell
# 1. Проверить, корректно ли работают сервисы
.\install_win.ps1 verify

# 2. Проверить настройки брандмауэра (Настройки брандмауэра Windows)

# 3. Проверить журналы сервисов
.\install_win.ps1 logs

# 4. Проверить статус контейнеров
docker ps
```

### 6. Потеря данных

**Проблема**: Потеря данных после очистки сервисов.

**Пояснение**: Команда `clean` удаляет все тома данных, что приводит к потере данных. Это ожидаемое поведение.

**Меры предосторожности**:
- Сделайте резервную копию важных данных перед выполнением `clean`.
- Используйте команду `clean` с осторожностью в рабочей среде.
- Рекомендуется использовать инструменты резервного копирования томов данных.

## Управление журналами

### Расположение файлов журналов

Журналы выполнения скриптов сохраняются в директории `.scripts/docker/logs/`:

- **Linux**: `install_linux_YYYYMMDD_HHMMSS.log`
- **macOS**: `install_mac_YYYYMMDD_HHMMSS.log`
- **Windows**: `install_win_YYYYMMDD_HHMMSS.log`

Имя файла журнала содержит временную метку для удобного различения записей различных выполнений.

### Просмотр журналов

#### Просмотр журналов выполнения скриптов

**Linux/macOS**:
```bash
# Посмотреть последние файлы журналов
ls -lt .scripts/docker/logs/ | head -5

# Посмотреть конкретный файл журнала
tail -f .scripts/docker/logs/install_linux_20240101_120000.log    # Linux
tail -f .scripts/docker/logs/install_mac_20240101_120000.log      # macOS
```

**Windows**:
```powershell
# Посмотреть последние файлы журналов
Get-ChildItem .scripts\docker\logs\ | Sort-Object LastWriteTime -Descending | Select-Object -First 5

# Посмотреть конкретный файл журнала
Get-Content .scripts\docker\logs\install_win_20240101_120000.log -Wait
```

#### Просмотр журналов сервисных контейнеров

**Linux/macOS**:
```bash
# Посмотреть журналы всех сервисов
./install_linux.sh logs    # Linux
./install_mac.sh logs      # macOS

# Посмотреть журналы конкретного сервиса (требуется перейти в директорию модуля)
cd DEVICE
docker-compose logs -f
```

**Windows**:
```powershell
# Посмотреть журналы всех сервисов
.\install_win.ps1 logs

# Посмотреть журналы конкретного сервиса (требуется перейти в директорию модуля)
cd DEVICE
docker-compose logs -f
```

### Содержимое журналов

Журналы скриптов содержат:
- Временную метку выполнения.
- Выполненные команды.
- Результаты выполнения каждого модуля.
- Сообщения об ошибках и предупреждениях.
- Информацию о статусе сервисов.

## Рекомендуемый процесс развертывания

### Первое развертывание

#### Linux

1. **Подготовка окружения**
   ```bash
   # Проверить системные требования
   uname -a
   free -h
   df -h

   # Установить Docker и Docker Compose
   # Ссылка: https://docs.docker.com/get-docker/
   ```

2. **Получение кода**
   ```bash
   git clone <repository-url>
   cd easyaiot
   ```

3. **Выполнение установки**
   ```bash
   cd .scripts/docker
   chmod +x install_linux.sh
   ./install_linux.sh install
   ```

4. **Проверка развертывания**
   ```bash
   ./install_linux.sh verify
   ```

5. **Доступ к сервисам**
   - Откройте браузер и перейдите по адресам сервисов.
   - Проверьте, корректно ли работают сервисы.

#### macOS

1. **Подготовка окружения**
   ```bash
   # Проверить системные требования
   uname -a
   system_profiler SPHardwareDataType | grep Memory
   df -h

   # Установить Docker Desktop for Mac
   # Ссылка: https://docs.docker.com/desktop/install/mac-install/
   ```

2. **Получение кода**
   ```bash
   git clone <repository-url>
   cd easyaiot
   ```

3. **Выполнение установки**
   ```bash
   cd .scripts/docker
   chmod +x install_mac.sh
   ./install_mac.sh install
   ```

4. **Проверка развертывания**
   ```bash
   ./install_mac.sh verify
   ```

5. **Доступ к сервисам**
   - Откройте браузер и перейдите по адресам сервисов.
   - Проверьте, корректно ли работают сервисы.

#### Windows

Для развертывания в Windows обратитесь к специальному [Руководству по развертыванию Windows](平台Windows部署文档_ru.md), которое содержит подробные пошаговые инструкции. Руководство охватывает сценарии локального развертывания на системах Windows 10/11, включая:

- Подготовку окружения и системные требования
- Установку и настройку промежуточного программного обеспечения
- Процедуры запуска сервисов
- Настройку видеопотоков
- Устранение неполадок и распространенные проблемы

**Примечание**: Руководство по развертыванию Windows в основном посвящено локальному развертыванию. Для развертывания на основе Docker в Windows можно использовать скрипт `install_win.ps1`, но подробные инструкции доступны в руководстве по развертыванию Windows.

### Ежедневное обслуживание

#### Linux/macOS

1. **Запуск сервисов**
   ```bash
   ./install_linux.sh start    # Linux
   ./install_mac.sh start      # macOS
   ```

2. **Остановка сервисов**
   ```bash
   ./install_linux.sh stop    # Linux
   ./install_mac.sh stop      # macOS
   ```

3. **Перезапуск сервисов**
   ```bash
   ./install_linux.sh restart    # Linux
   ./install_mac.sh restart      # macOS
   ```

4. **Просмотр статуса**
   ```bash
   ./install_linux.sh status    # Linux
   ./install_mac.sh status      # macOS
   ```

5. **Просмотр журналов**
   ```bash
   ./install_linux.sh logs    # Linux
   ./install_mac.sh logs      # macOS
   ```

#### Windows

1. **Запуск сервисов**
   ```powershell
   .\install_win.ps1 start
   ```

2. **Остановка сервисов**
   ```powershell
   .\install_win.ps1 stop
   ```

3. **Перезапуск сервисов**
   ```powershell
   .\install_win.ps1 restart
   ```

4. **Просмотр статуса**
   ```powershell
   .\install_win.ps1 status
   ```

5. **Просмотр журналов**
   ```powershell
   .\install_win.ps1 logs
   ```

### Обновление развертывания

#### Linux/macOS

1. **Получение последнего кода**
   ```bash
   git pull
   ```

2. **Обновление сервисов**
   ```bash
   cd .scripts/docker
   ./install_linux.sh update    # Linux
   ./install_mac.sh update      # macOS
   ```

3. **Проверка обновления**
   ```bash
   ./install_linux.sh verify    # Linux
   ./install_mac.sh verify      # macOS
   ```

#### Windows

1. **Получение последнего кода**
   ```powershell
   git pull
   ```

2. **Обновление сервисов**
   ```powershell
   cd .scripts\docker
   .\install_win.ps1 update
   ```

3. **Проверка обновления**
   ```powershell
   .\install_win.ps1 verify
   ```

## Важные замечания

1. **Требования к версиям**: **Обязательно** установите Docker v29.0.0+ и Docker Compose v2.35.0+, более ранние версии не будут работать корректно.
2. **Сетевые требования**: Убедитесь, что сервер имеет доступ к Docker Hub или настроенному реестру образов.
3. **Требования к ресурсам**: Убедитесь, что у сервера достаточно CPU, памяти и дискового пространства.
4. **Конфликт портов**: Убедитесь, что необходимые порты не заняты другими сервисами.
5. **Резервное копирование данных**: Перед развертыванием в рабочей среде создайте резервные копии важных данных.
6. **Настройка безопасности**: В рабочей среде настройте правила брандмауэра и групп безопасности.
7. **Управление журналами**: Регулярно очищайте старые файлы журналов, чтобы избежать нехватки дискового пространства.

## Техническая поддержка

Если у вас возникли проблемы, пожалуйста:

1. Ознакомьтесь с разделом [Частые проблемы](#частые-проблемы) в этом документе.
2. Посмотрите журналы сервисов: `./install_all.sh logs`.
3. Проверьте статус Docker: `docker ps -a`.
4. Создайте Issue в репозитории проекта.

---

**Версия документа**: 1.0  
**Последнее обновление**: 2024-01-01  
**Расположение скрипта**: `.scripts/docker/install_all.sh`